# Copie para .env e preencha

# Chave secreta para JWT (produção: use openssl rand -hex 32)
SECRET_KEY=altere-em-producao-use-openssl-rand-hex-32

# OpenAI (obrigatório para gerar artigos)
OPENAI_API_KEY=sk-...

# Banco (SQLite por padrão; depois pode usar PostgreSQL)
# DATABASE_URL=sqlite+aiosqlite:///./artigos.db
# DATABASE_URL=postgresql+asyncpg://user:pass@localhost/dbname

# Opcional
# DEBUG=false
# ACCESS_TOKEN_EXPIRE_MINUTES=1440
